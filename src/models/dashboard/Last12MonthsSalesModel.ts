export interface Last12MonthsSalesModel {
    monthLabel: string;   // e.g., "January 2026"
    totalSale: number;    // Total sale for the month
    cashUpi: number;      // Total Cash + UPI
    credit: number;       // Total Credit
}
